import React from 'react'

import { css } from '@emotion/core'
import styled from '@emotion/styled'

const Wrapper = styled.div(css`
  width: 20%;
  display: inline-block;
`)

export const Room = () => (
  <Wrapper>Users go here</Wrapper>
)
